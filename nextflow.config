manifest {
    name = "Rob Syme test template"
}

process.container = 'quay.io/nextflow/bash'

workflow.onComplete = {
    sleep 1000
    println("The secret in onComplete via config: ${secrets.TMPSECRET}")
}