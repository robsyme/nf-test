manifest {
    name = "Rob Syme test template"
}

params {
    name = "Default in params block"
}

profiles {
    test {
        params.name = 'Value set in profile'
    }
}

process.container = 'quay.io/nextflow/bash'
