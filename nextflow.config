manifest {
    name = 'Network data transfer testing'
    nextflowVersion = '22.10.0'
}

params {
    outdir = 'results'
    filesize = '10M'
    count = 10
}

process {
    container = 'quay.io/nextflow/bash'
}
