process.container = 'quay.io/nextflow/bash'

params.outdir = "s3://scidev-testing/projects/reports-2320/results"

manifest {
    name = 'Tower reports publishing tests'
    description = 'Test alternatives for publishing files deep in output directories'
}
