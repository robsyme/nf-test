manifest {
    name = "Fusion writeback probe: when does written data reach S3?"
}

// Trace-level Fusion logging, so .fusion.log is a second view on the same
// window as the S3 polls.
env.FUSION_LOG_LEVEL = 'trace'
