manifest {
    name = "Rob Syme test template"
}


process.container = 'quay.io/nextflow/bash'

params {
    one = "first"
    two = "${params.one}/two"
    three = "${params.two}/three"
    four = "${params.three}/four"
}