manifest.name = "Fusion debugging"
wave.enabled = true
fusion.enabled = true
process.container = 'quay.io/biocontainers/fastp:0.23.2--h79da9fb_0'

profile {
    docker {
        docker {
        enabled = true
        runOptions = '-v $HOME/.aws:/root/.aws'
        envWhitelist = 'AWS_PROFILE'
        }
    }
}