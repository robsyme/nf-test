process.container = 'interlinetx/gergo.model:b8780492'

docker {
    runOptions= "--gpus all --runtime=nvidia -e NVIDIA_DRIVER_CAPABILITIES=compute,utility -e NVIDIA_VISIBLE_DEVICES=all"
    userEmulation = true
    enabled = true
}

params {
    // Input options
    interactions                = null
    embeddings                  = null
    weights                     = null
    s3_publish                  = null
}
