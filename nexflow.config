process.container = 'quay.io/nextflow/bash'

manifest {
    name = 'Testing publication options'
    homePage = 'https://github.com/robsyme/nf-test'
    description = 'Publishes csv files and tries to catch them using tower.yml'
}
