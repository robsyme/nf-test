{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://raw.githubusercontent.com/Rob Syme test template/master/nextflow_schema.json",
  "title": "Rob Syme test template pipeline parameters",
  "description": "",
  "type": "object",
  "definitions": {
    "basic_options": {
      "title": "Basic Options",
      "type": "object",
      "description": "",
      "default": "",
      "properties": {
        "memory": {
          "type": "string",
          "default": "1 GB",
          "description": "How much memory to allocate"
        },
        "time": {
          "type": "string",
          "default": "1 min",
          "description": "How long to hold the data in memory"
        }
      },
      "required": ["memory", "time"]
    }
  },
  "allOf": [
    {
      "$ref": "#/definitions/basic_options"
    }
  ]
}
